# Stable versions for Multi-Agent RL (Python 3.11 compatible)
ray[rllib]==2.6.1
gymnasium==0.28.1
torch>=2.0.0
numpy<2.0.0
pandas
matplotlib
pettingzoo>=1.24.0
# Override Ray's PyArrow requirement with Python 3.11 compatible version
pyarrow>=12.0.0
